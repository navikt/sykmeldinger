mutation Dev_ChangeUserScenario($scenario: String!) {
    dev_changeScenario(scenario: $scenario)
}

mutation Dev_SetAntallArbeidsgivere($antall: Int!) {
    dev_setAntallArbeidsgivere(antall: $antall)
}
