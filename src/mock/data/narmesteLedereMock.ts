import NaermesteLeder from "../../types/naermesteLederTypes";

const naermesteLedereMock = Object.assign(new NaermesteLeder(), {
    navn: "pontypandy fireservice"
})

export default naermesteLedereMock